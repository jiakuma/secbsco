# 产品需求文档（PRD）

## 生物安全数据联合统计系统

---

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | 生物安全数据联合统计系统（Biosecurity Federated Statistics System） |
| 文档版本 | V1.0 |
| 文档状态 | 正式版 |
| 创建日期 | 2026-05-26 |
| 作者 | CodeArts（反向推导自代码仓库） |
| 文档说明 | 基于现有代码库（FastAPI + Vue 3 + MySQL）反向推导生成的产品需求文档 |

---

## 2. 背景与目标

### 2.1 业务背景

在生物安全与传染病监测领域，各级疾控中心（CDC）、医院、实验室等机构掌握着大量敏感的病例数据与流行病学数据。由于数据涉及个人隐私与生物安全合规要求，各机构之间无法直接共享原始数据，导致：

- **跨区域传染病态势感知滞后**：单一机构数据无法反映区域整体疫情趋势
- **联合统计分析困难**：传统方式需集中汇聚原始数据，违反数据不出域原则
- **疫情溯源缺乏协作**：跨区县传播链分析需要多方数据协同，但缺乏安全协作机制
- **过程审计与信任缺失**：联合统计过程无法被第三方审计验证，机构间缺乏信任基础

### 2.2 产品目标

构建一个**隐私计算+区块链存证**一体化平台，实现：

1. **数据可用不可见**：基于 SecretFlow 隐私计算框架，支持多机构在不共享原始数据的前提下完成联合统计与联邦学习
2. **全流程可审计可溯源**：集成 FISCO BCOS 区块链，对任务结果与操作日志进行链上存证，确保数据不可篡改、过程可追溯
3. **多级组织协同治理**：支持平台-机构-群组三级组织架构与 RBAC 权限模型，实现跨层级的数据协作管控
4. **多场景任务支撑**：覆盖联合病例统计、跨区县传染病时空预测、疫苗效果评估等核心业务场景

---

## 3. 用户角色与场景

### 3.1 用户角色定义

| 角色 | 角色编码 | 典型身份 | 作用域 | 核心职责 |
|------|----------|----------|--------|----------|
| 平台管理员 | `admin` + `platform` | 省级/国家级疾控中心管理员 | 全平台 | 管理所有机构、用户、群组；系统级配置；审批群组创建/解散 |
| 机构管理员 | `admin` + `agency` | 市级/区县级疾控中心管理员 | 本机构及下级 | 管理本机构用户与节点；参与/创建群组；审批下级机构群组申请 |
| 群组管理员 | `admin` + `group` | 群组牵头机构指定管理员 | 所属群组 | 管理群组成员与资源授权；分配群组用户角色；创建/执行群组任务 |
| 群组业务用户 | `user` + `group` | 疾控中心业务分析师 | 所属群组 | 查看群组任务与结果；创建联合统计任务；执行任务 |
| 区块链治理员 | `governor` + `group` | 审计/监管人员 | 所属群组 | 查看任务结果；执行链上存证校验；管理合约；查看审计日志（不可执行任务） |

### 3.2 核心业务场景

#### 场景一：T1-联合病例统计（多方安全计算）

- **触发条件**：某群组管理员或业务用户需要汇总多个机构的传染病病例数据（如 ILI 流感样病例统计）
- **前置条件**：群组已激活；参与机构已授权数据集；统计模板已配置
- **主流程**：
  1. 用户选择群组与统计模板，填写统计参数（时间范围、症候群类型等）
  2. 配置参与方：为每个参与机构选择执行节点、数据集与字段映射
  3. 提交执行：系统调用 SecretFlow 联合统计服务进行多方安全计算
  4. 返回统计结果：汇总各参与方脱敏统计值，生成联合统计报告
  5. 结果存证：将任务结果哈希锚定到 FISCO BCOS 区块链
- **后置条件**：结果可查看、可导出、可链上校验

#### 场景二：T2-跨区县传染病时空预测与疫情溯源（联邦学习）

- **触发条件**：需要预测跨区县传染病传播趋势与时空溯源
- **前置条件**：多个区县节点已激活并部署联邦学习运行时
- **主流程**：
  1. 创建联邦学习任务：配置训练参数（模型类型、轮次、学习率等）与隐私参数（安全聚合、DP差分隐私等）
  2. 配置训练节点：为每个参与方配置特征字段映射（case_id_hash、disease_code、spatial_grid_id 等）
  3. 执行训练：调用 SecretFlow 联邦训练服务，各节点本地训练后安全聚合全局模型
  4. 返回训练结果：各轮 loss/accuracy/auc 指标、最终模型摘要、隐私求交溯源分析
  5. 结果存证与审计
- **约束**：原始数据不上传（`raw_data_export: false`），仅交换模型参数/梯度摘要

#### 场景三：T3-疫苗效果评估

- **触发条件**：需要基于多机构脱敏数据评估疫苗保护效果
- **前置条件**：群组内已授权疫苗相关数据集
- **主流程**：创建疫苗效果评估任务 -> 配置参与方与数据集 -> 执行隐私计算 -> 生成评估报告
- **输出**：疫苗保护率、有效率等统计指标

#### 场景四：群组全生命周期管理

- **触发条件**：多个机构需要建立协作关系进行联合统计
- **主流程**：
  1. 牵头机构创建群组，添加成员机构
  2. 上级机构或平台管理员审批
  3. 审批通过后群组激活，管理员授权节点/数据集/模板
  4. 群组内创建并执行联合统计任务
  5. 群组解散：发起解散申请 -> 审批 -> 归档

#### 场景五：节点激活与管理

- **触发条件**：新机构节点部署完成，需要接入平台
- **主流程**：
  1. 管理员注册节点（配置服务地址、Agent 地址、节点能力等）
  2. 检测 Agent 状态：系统调用节点 Agent 检查服务是否可用
  3. 激活节点：通过 Agent 启动节点服务
  4. 节点正常运行，可被群组授权使用

#### 场景六：区块链存证与校验

- **触发条件**：需要对任务结果或审计日志进行不可篡改的存证
- **主流程**：
  1. 对任务结果/审计日志计算内容哈希
  2. 调用 FISCO BCOS 存证锚定服务，将哈希写入区块链
  3. 记录交易哈希、区块高度、合约地址
  4. 后续可进行链上校验：对比本地数据哈希与链上哈希，验证数据一致性

---

## 4. 功能需求

### 4.1 认证与访问控制模块

#### FR-AUTH-01 用户登录

- **用户故事**：作为系统用户，我希望通过用户名和密码登录系统，以便访问我有权限的功能
- **验收标准**：
  - 输入正确的用户名和密码，返回 JWT Token（HS256，有效期 24 小时）
  - 用户状态为 `disabled`/`locked`/`archived` 时拒绝登录
  - 登录成功后记录操作日志（IP、时间）
  - 登录失败返回明确的错误提示

#### FR-AUTH-02 动态菜单

- **用户故事**：作为不同角色的用户，我希望登录后看到与我角色匹配的菜单，以便快速访问相关功能
- **验收标准**：
  - 平台管理员：首页、任务、结果、机构与节点、群组管理、用户管理、角色授权
  - 机构管理员：首页、任务、结果、机构与节点、用户管理
  - 群组管理员：首页、任务、模板、结果、机构与节点、群组管理、用户管理
  - 群组业务用户：首页、任务、模板、结果、审计查询
  - 区块链治理员：首页、结果、区块链治理、存证记录、链上校验、合约管理

#### FR-AUTH-03 RBAC 权限控制

- **用户故事**：作为系统管理员，我希望通过角色-作用域绑定精确控制用户权限，确保数据安全
- **验收标准**：
  - 支持三种角色：`admin`、`user`、`governor`
  - 支持三种作用域：`platform`（全平台）、`agency`（机构级）、`group`（群组级）
  - 用户可同时拥有多个角色-作用域绑定
  - 数据可见性根据作用域自动过滤（如机构管理员仅可见本机构及下级数据）
  - 区块链治理员不可执行任务

### 4.2 用户管理模块

#### FR-USER-01 用户 CRUD

- **用户故事**：作为管理员，我希望管理系统用户，包括创建、查看、编辑、启用/禁用/删除
- **验收标准**：
  - 支持分页列表查询，可按关键词、状态、机构、角色过滤
  - 创建用户时指定所属机构
  - 支持启用/禁用/删除操作
  - 密码使用 bcrypt 哈希存储

#### FR-USER-02 用户角色绑定

- **用户故事**：作为管理员，我希望为用户分配角色并指定作用域
- **验收标准**：
  - 支持为用户绑定多个角色
  - 每个角色绑定指定作用域类型（platform/agency/group）和作用域 ID
  - 同一用户-角色-作用域组合唯一
  - 支持解绑角色

#### FR-USER-03 用户群组管理

- **用户故事**：作为管理员，我希望将用户添加到群组或从群组移除
- **验收标准**：
  - 支持将用户添加到群组并指定所属机构
  - 支持从群组移除用户
  - 用户可属于多个群组

### 4.3 机构管理模块

#### FR-AGENCY-01 机构 CRUD

- **用户故事**：作为平台管理员，我希望管理系统中所有参与机构
- **验收标准**：
  - 支持机构编码（唯一）、名称、类型、级别、上级机构、行政区划等字段
  - 机构类型：`cdc`（疾控中心）、`hospital`（医院）、`lab`（实验室）、`health_commission`（卫健委）、`research`（科研机构）、`other`
  - 机构级别：`county`/`city`/`province`/`national`
  - 支持树形结构展示（基于 parent_agency_id）
  - 支持启用/禁用/删除，删除时级联清理关联数据

### 4.4 节点管理模块

#### FR-NODE-01 节点 CRUD

- **用户故事**：作为管理员，我希望管理各机构部署的计算/服务/区块链节点
- **验收标准**：
  - 节点编码唯一，每个节点归属于一个机构
  - 节点类型：`integrated_node`/`service_node`/`data_node`/`compute_node`/`blockchain_node`/`gateway_node`
  - 节点能力（JSON）：`data`/`compute`/`service`/`chain`
  - 支持配置网络地址（服务URL、内网/公网IP）、Ray 集群地址
  - 支持配置区块链参数（链类型、节点ID、RPC/P2P 端点）
  - 支持配置存证参数（锚定服务URL、合约地址、证书ID）
  - 支持配置 Agent 控制参数（Agent URL、Agent Token）
  - 支持资源配置（CPU/内存/GPU 总量、最大并发任务数）

#### FR-NODE-02 节点状态管理

- **用户故事**：作为管理员，我希望监控和管理节点的运行状态
- **验收标准**：
  - 节点注册状态：`registered`/`checking`/`active`/`offline`/`disabled`/`failed`
  - 节点激活状态：`not_activated`/`activating`/`activated`/`activation_failed`
  - 节点负载状态：`idle`/`busy`/`offline`/`disabled`
  - 支持启用/禁用/删除操作

#### FR-NODE-03 节点 Agent 检测与激活

- **用户故事**：作为管理员，我希望通过 Agent 远程检测节点状态并激活/停用节点
- **验收标准**：
  - 检测：调用节点 Agent URL，返回 Agent 服务可用性
  - 激活：通过 Agent 启动节点服务，激活状态变为 `activated`
  - 停用：通过 Agent 停止节点服务，激活状态变为 `not_activated`

### 4.5 数据集管理模块

#### FR-DATASET-01 数据集 CRUD

- **用户故事**：作为管理员，我希望管理各机构的数据资源
- **验收标准**：
  - 数据集编码唯一，归属于一个机构和一个节点
  - 数据类型：`file`（文件）、`database`（数据库）、`api`（接口）
  - 支持配置数据位置、存储 URI、字段结构（schema_json）
  - 支持数据集类型（扩展字段）和版本号
  - 支持启用/禁用状态

### 4.6 统计模板管理模块

#### FR-TEMPLATE-01 统计模板 CRUD

- **用户故事**：作为管理员，我希望管理预定义的统计模板，供任务创建时选用
- **验收标准**：
  - 模板编码唯一
  - 支持配置：适用场景、执行模式（auto/manual）、输出类型、统计类型
  - 支持配置指标结构（metrics_json）、参数结构（params_schema_json）
  - 支持配置执行器配置（executor_config_json）、输入要求（input_requirements_json）
  - 核心模板不可删除（受保护）
  - 支持模板哈希（template_hash）用于完整性校验

### 4.7 群组管理模块

#### FR-GROUP-01 群组 CRUD

- **用户故事**：作为机构管理员，我希望创建和管理协作群组
- **验收标准**：
  - 群组编码唯一
  - 群组级别：`county`/`city`/`province`/`national`
  - 指定牵头机构（lead_agency_id）
  - 群组状态：`draft` -> `pending_approval` -> `active` -> `dissolving` -> `dissolved` -> `archived`
  - 支持是否需要审批（approval_required）和审批状态流转

#### FR-GROUP-02 群组审批

- **用户故事**：作为上级管理员，我希望审批群组的创建和删除申请
- **验收标准**：
  - 创建审批：通过/驳回，审批后群组状态变为 `active`/`rejected`
  - 删除审批：申请删除 -> 审批通过删除/驳回删除
  - 审批由共同上级机构或平台管理员执行

#### FR-GROUP-03 群组成员管理

- **用户故事**：作为群组管理员，我希望管理群组的成员机构
- **验收标准**：
  - 支持添加/移除成员机构
  - 成员角色：`lead_agency`/`participant`/`data_provider`/`compute_provider`/`observer`
  - 标记牵头机构（is_lead）
  - 成员加入状态：`pending`/`active`/`removed`/`disabled`/`archived`

#### FR-GROUP-04 群组用户管理

- **用户故事**：作为群组管理员，我希望管理群组内的用户及其角色
- **验收标准**：
  - 支持添加/移除群组用户
  - 支持修改用户在群组中的角色
  - 用户归属于特定机构

#### FR-GROUP-05 群组资源授权

- **用户故事**：作为群组管理员，我希望为群组授权可使用的节点、数据集和模板
- **验收标准**：
  - 节点授权：支持查看已授权/可授权节点，授权/撤销节点
  - 节点用途角色：`group_service`/`group_data`/`group_compute`/`group_blockchain`
  - 节点授权支持资源配额（resource_quota_json）
  - 数据集授权：支持查看已授权/可授权数据集，授权/撤销数据集
  - 模板授权：支持查看已授权/可授权模板，授权/撤销模板

#### FR-GROUP-06 群组生命周期日志

- **用户故事**：作为管理员，我希望查看群组的完整生命周期操作记录
- **验收标准**：
  - 记录所有群组状态变更事件
  - 包含变更前/后状态、操作人、原因、详情
  - 支持按群组查询日志列表

### 4.8 任务管理模块

#### FR-TASK-01 任务 CRUD

- **用户故事**：作为群组用户，我希望创建和管理联合统计/联邦学习任务
- **验收标准**：
  - 任务编码唯一，归属于创建用户、创建机构和群组
  - 关联统计模板
  - 支持配置统计时间范围、任务参数（params_json，含 task_type 等）
  - 支持选择牵头机构和执行模式（auto/manual）
  - 任务状态：`created`/`running`/`success`/`failed`/`cancelled`
  - 任务列表根据用户权限过滤可见性

#### FR-TASK-02 任务参与方管理

- **用户故事**：作为任务创建者，我希望为任务配置参与机构、执行节点和数据集
- **验收标准**：
  - 每个参与方关联：任务、机构、节点、数据集
  - 支持配置参与方角色（party_role）
  - 支持字段映射（field_mapping_json）：定义参与方数据字段到模板字段的映射关系
  - 参与方状态：`pending`/`running`/`success`/`failed`

#### FR-TASK-03 任务执行

- **用户故事**：作为群组业务用户，我希望执行联合统计或联邦学习任务
- **验收标准**：
  - 区块链治理员不可执行任务
  - T1 联合统计：调用 SecretFlow 联合统计服务（端口 18180）执行 `/run/flu-stat`
  - T2 联邦学习：调用 SecretFlow 联邦训练服务（端口 18181）执行 `/train/flu-fl`
  - 任务状态从 `created` -> `running` -> `success`/`failed`
  - 执行失败时记录错误信息
  - 支持 Mock 执行模式（用于开发测试）

#### FR-TASK-04 任务结果查看

- **用户故事**：作为用户，我希望查看任务执行结果
- **验收标准**：
  - 结果包含：result_json（结果数据）、metrics_json（指标明细）、result_hash（结果哈希）
  - 支持按任务类型区分结果展示视图：
    - T1 联合统计：统计结果表格/图表
    - T2 联邦学习：训练轮次曲线（loss/accuracy/auc）、最终指标、隐私求交溯源分析
    - T3 疫苗效果评估：评估指标报告
    - T4 其他扩展类型
  - 结果版本号管理

### 4.9 区块链存证与校验模块

#### FR-CHAIN-01 任务结果存证

- **用户故事**：作为区块链治理员，我希望将任务结果锚定到区块链，确保不可篡改
- **验收标准**：
  - 对任务结果计算内容哈希
  - 调用 FISCO BCOS 存证锚定服务（端口 18080）写入区块链
  - 记录：业务类型（task_result）、业务 ID、内容哈希、交易哈希、区块高度、合约地址
  - 更新任务结果的上链状态：`none` -> `pending` -> `success`/`failed`

#### FR-CHAIN-02 审计日志存证

- **用户故事**：作为区块链治理员，我希望将关键审计日志上链存证
- **验收标准**：
  - 对审计日志计算内容哈希
  - 调用存证服务写入区块链
  - 记录业务类型为 `audit_log`

#### FR-CHAIN-03 链上校验

- **用户故事**：作为区块链治理员，我希望校验本地数据与链上数据的一致性
- **验收标准**：
  - 校验状态：`unverified` -> `verify_success`/`local_data_changed`/`chain_mismatch`/`chain_not_found`/`query_failed`
  - 本地数据变更时标记为 `local_data_changed`
  - 链上数据不匹配时标记为 `chain_mismatch`

#### FR-CHAIN-04 存证记录查询

- **用户故事**：作为用户，我希望查看存证记录列表和详情
- **验收标准**：
  - 支持分页查询存证记录
  - 显示业务类型、业务 ID、交易哈希、区块高度、合约地址、校验状态

#### FR-CHAIN-05 合约管理

- **用户故事**：作为区块链治理员，我希望管理链上智能合约信息
- **验收标准**：
  - 支持查看合约列表：合约名称、版本、地址、链类型
  - 合约状态：`active`/`disabled`/`archived`

### 4.10 审计日志模块

#### FR-AUDIT-01 操作日志记录

- **用户故事**：作为系统，我希望自动记录所有关键操作，以便后续审计
- **验收标准**：
  - 自动记录用户操作日志：操作人、操作类型、资源类型/ID、请求信息、结果状态、IP 地址
  - 关联群组和机构信息
  - 支持分页查询审计日志
  - 支持查看日志详情（请求参数、返回结果）

### 4.11 仪表盘模块

#### FR-DASH-01 统计总览

- **用户故事**：作为用户，我希望在首页看到系统关键指标的汇总信息
- **验收标准**：
  - 统计卡片：任务数、结果数、机构数、节点数等
  - 最近任务列表
  - 最近结果列表
  - 最近审计日志
  - 最近存证记录

---

## 5. 非功能需求

### 5.1 性能要求

| 指标 | 要求 |
|------|------|
| 页面首屏加载 | < 3 秒 |
| API 接口响应 | < 500ms（非计算类接口） |
| 任务列表查询 | 支持分页，默认每页 20 条 |
| 数据库连接池 | pool_pre_ping=True，pool_recycle=3600s |

### 5.2 安全要求

| 指标 | 要求 |
|------|------|
| 认证方式 | JWT Bearer Token（HS256 签名） |
| Token 有效期 | 1440 分钟（24 小时） |
| 密码存储 | bcrypt 哈希，不可逆 |
| 权限模型 | RBAC + 作用域（platform/agency/group） |
| 数据隔离 | 基于用户角色和作用域自动过滤数据可见性 |
| 跨域安全 | 前端 Axios 拦截器自动处理 401/403 |
| 隐私保护 | 原始数据不出域，仅交换脱敏统计值或模型参数 |

### 5.3 可用性要求

| 指标 | 要求 |
|------|------|
| 健康检查 | 提供 `/api/health` 和 `/api/health/db` 端点 |
| 错误处理 | 统一响应格式 `{code, message, data}`，code=0 成功，非0 失败 |
| 节点容错 | 节点离线时不影响其他节点任务执行 |

### 5.4 兼容性要求

| 指标 | 要求 |
|------|------|
| 浏览器 | Chrome 90+、Edge 90+、Firefox 90+ |
| 数据库 | MySQL 8.x |
| Python | 3.10+ |
| Node.js | 18+ |

---

## 6. 交互与原型规格

### 6.1 整体布局

- **主布局**：左侧导航栏 + 顶部头部栏 + 右侧内容区
- **导航栏**：根据用户角色动态生成菜单项
- **头部栏**：显示当前用户信息、退出登录
- **内容区**：Vue Router 路由切换

### 6.2 核心页面交互

| 页面 | 关键交互 |
|------|----------|
| 登录页 | 用户名+密码表单，登录后跳转仪表盘 |
| 仪表盘 | 统计卡片 + 最近记录列表，ECharts 图表 |
| 任务列表 | 分页表格，支持按状态/类型/群组过滤，操作按钮（查看/执行/删除） |
| 任务详情 | 基本信息编辑 + 参与方配置面板 + 执行按钮 + 状态展示 |
| 任务结果 | 根据任务类型（T1/T2/T3/T4）动态切换结果视图；T2 含联邦学习 3D 动画（Three.js）与训练曲线图（ECharts） |
| 群组列表 | 分页表格，支持审批/查看操作 |
| 群组详情 | Tab 切换：基本信息/成员机构/群组用户/节点授权/数据集授权/模板授权/生命周期日志 |
| 节点管理 | 分页表格，支持激活/停用/检测 Agent 操作 |
| 区块链治理 | 存证记录列表 + 链上校验操作 + 合约管理 |

### 6.3 前端技术约定

- 框架：Vue 3 + TypeScript + Composition API
- UI 库：Element Plus
- 状态管理：Pinia
- 图表：ECharts 6.x
- 3D 动画：Three.js 0.184.x
- HTTP 客户端：Axios（自动注入 JWT Bearer Token）
- 命名约定：前端 camelCase（handleRun, taskId），API 参数 snake_case（task_id, result_hash）

---

## 7. 数据需求

### 7.1 核心数据实体

| 实体 | 表名 | 核心字段 | 记录量级 |
|------|------|----------|----------|
| 系统用户 | `sys_user` | username, real_name, agency_id, status | 百级 |
| 角色 | `sys_role` | role_code, role_name | 个级（3个） |
| 用户角色绑定 | `sys_user_role_binding` | user_id, role_code, scope_type, scope_id | 百级 |
| 用户群组关系 | `sys_user_group` | user_id, group_id, agency_id | 百级 |
| 操作日志 | `sys_user_operate_log` | user_id, operation_type, resource_type | 万级 |
| 机构 | `agency` | agency_code, agency_name, agency_type, parent_agency_id | 百级 |
| 节点 | `node` | node_code, node_name, agency_id, node_type, activation_status | 百级 |
| 数据集 | `dataset` | dataset_code, dataset_name, agency_id, node_id | 千级 |
| 统计模板 | `stat_template` | template_code, template_name, scenario | 十级 |
| 任务 | `task` | task_code, task_name, template_id, group_id, status | 千级 |
| 任务参与方 | `task_party` | task_id, agency_id, node_id, dataset_id | 千级 |
| 任务结果 | `task_result` | task_id, result_json, result_hash, anchor_status | 千级 |
| 审计日志 | `audit_log` | task_id, operation_type, operation_desc | 万级 |
| 存证记录 | `chain_record` | biz_type, biz_id, tx_hash, block_number | 千级 |
| 合约信息 | `contract_info` | contract_name, contract_address | 个级 |
| 群组 | `group_info` | group_code, group_name, lead_agency_id, status | 十级 |
| 群组成员 | `group_member` | group_id, agency_id, member_role | 百级 |
| 群组节点授权 | `group_node` | group_id, agency_id, node_id, node_usage_role | 百级 |
| 群组数据集授权 | `group_dataset` | group_id, agency_id, dataset_id | 百级 |
| 群组模板授权 | `group_task_template` | group_id, agency_id, template_id | 百级 |
| 群组生命周期日志 | `group_lifecycle_log` | group_id, event_type, before_status, after_status | 千级 |

### 7.2 数据库要求

- 数据库：MySQL 8.x，数据库名 `biosecurity_stat`
- 字符集：utf8mb4
- 迁移工具：Alembic
- 所有逻辑删除使用 status 字段软删除
- JSON 字段用于存储灵活结构（params_json、schema_json、metrics_json 等）

---

## 8. 依赖与约束

### 8.1 外部服务依赖

| 服务 | 端口 | 用途 | 依赖程度 |
|------|------|------|----------|
| MySQL 8.x | 3306 | 业务数据存储 | 必须 |
| FISCO BCOS 存证锚定服务 | 18080 | 区块链存证写入 | 可降级（Mock） |
| SecretFlow 联合统计服务 | 18180 | T1 多方安全计算 | 可降级（Mock） |
| SecretFlow 联邦训练服务 | 18181 | T2 联邦学习训练 | 可降级（Mock） |
| Bio Task Runtime | 18190 | T2 时空轨迹预测运行时 | 可选 |
| Alice Node Agent | 19090 | 节点远程控制 | 可选 |

### 8.2 技术约束

| 约束 | 说明 |
|------|------|
| 数据不出域 | 各机构原始数据必须保留在本地节点，仅通过隐私计算协议交换脱敏结果 |
| 隐私计算框架 | 依赖 SecretFlow 作为隐私计算执行引擎 |
| 区块链类型 | 当前仅支持 FISCO BCOS |
| 节点通信 | 节点间通过 Alice 节点 Agent 进行远程控制 |
| 前后端分离 | 前端通过 REST API 与后端交互，不允许直接数据库访问 |

### 8.3 业务约束

| 约束 | 说明 |
|------|------|
| 群组必须审批 | 新建群组需经上级机构或平台管理员审批后方可激活 |
| 任务需群组上下文 | 任务必须归属于某个群组，使用群组授权的资源 |
| 治理员不可执行任务 | 区块链治理员仅可查看与校验，不可创建/执行任务 |
| 核心模板不可删除 | 系统预设的统计模板受保护，不可删除 |

---

## 9. 里程碑

| 阶段 | 里程碑 | 核心交付物 |
|------|--------|------------|
| M1 | 基础架构搭建 | 用户认证、RBAC、机构/节点/数据集 CRUD、数据库模型 |
| M2 | 群组管理 | 群组全生命周期、成员管理、资源授权、审批流程 |
| M3 | 任务与模板 | 统计模板管理、任务 CRUD、参与方配置、Mock 执行 |
| M4 | 隐私计算集成 | SecretFlow 联合统计/联邦学习真实执行、结果管理 |
| M5 | 区块链存证 | FISCO BCOS 存证锚定、链上校验、合约管理、审计日志上链 |
| M6 | 仪表盘与体验优化 | 仪表盘统计、ECharts 可视化、Three.js 联邦动画、全流程联调 |

---

## 10. 附录

### 10.1 API 响应格式

- 统一格式：`{ "code": 0, "message": "success", "data": {...} }`
- code=0 表示成功，非0 表示失败
- 分页响应：`{ "total": 100, "page": 1, "page_size": 20, "items": [...] }`

### 10.2 状态机定义

#### 群组状态机

`draft` -> `pending_approval` -> `active` / `rejected`
`active` -> `dissolving` -> `dissolved` -> `archived`
`active` -> `disabled`

#### 任务状态机

`created` -> `running` -> `success` / `failed`
`created` -> `cancelled`

#### 节点激活状态机

`not_activated` -> `activating` -> `activated` / `activation_failed`
`activated` -> `not_activated`（停用）

#### 存证上链状态机

`none` -> `pending` -> `success` / `failed`

#### 链上校验状态机

`unverified` -> `verify_success` / `local_data_changed` / `chain_mismatch` / `chain_not_found` / `query_failed`

### 10.3 技术栈汇总

| 层次 | 技术 |
|------|------|
| 前端框架 | Vue 3.5 + TypeScript + Composition API |
| 前端 UI | Element Plus 2.13 |
| 前端图表 | ECharts 6.x |
| 前端 3D | Three.js 0.184.x |
| 前端构建 | Vite 8.x |
| 前端状态 | Pinia 3.x |
| 前端路由 | Vue Router 4.x |
| 后端框架 | FastAPI 0.115.x |
| ORM | SQLAlchemy 2.x |
| 数据库 | MySQL 8.x + PyMySQL |
| 迁移 | Alembic |
| 认证 | python-jose (JWT) + passlib/bcrypt |
| 配置 | pydantic-settings 2.x |
| 服务器 | uvicorn 0.34.x |
| 隐私计算 | SecretFlow |
| 区块链 | FISCO BCOS |
