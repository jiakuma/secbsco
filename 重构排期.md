# DDD+六边形架构重构排期（3天冲刺版）

## 生物安全数据联合统计系统

---

## 总览

| 时间段 | 阶段 | 内容 | 产出 |
|--------|------|------|------|
| Day1 上午 | P0 | 骨架+共享内核+全部枚举+全部Port接口 | 目录结构就位，所有抽象定义完成 |
| Day1 下午 | P1+P2+P6+P9 | Template+Dataset+Chain+Computation（4个简单上下文批量迁移） | 4个上下文迁移完成 |
| Day2 上午 | P3+P4 | Agency+Node（中等复杂） | 2个上下文迁移完成 |
| Day2 下午 | P5+P7 | Identity+Audit（核心Port实现，解锁后续依赖） | AccessControlPort/AuditLogPort可用 |
| Day3 上午 | P8+P10 | Group+Task（最复杂两个上下文） | 核心业务迁移完成 |
| Day3 下午 | P11+P12+P13+P14 | API瘦化+ORM隔离+事件+清理回归 | 全部完成 |

---

## Day1 上午（4h）：骨架 + 抽象定义

> AI批量生成所有枚举、Port接口、领域异常，一次性到位

### 任务

| # | 任务 | 产出 |
|---|------|------|
| 1 | 创建全部目录结构 | `shared/` + 10个 `contexts/` 各含 `domain/application/adapters/` + `infrastructure/` |
| 2 | `shared/hashing.py` — 统一 `compute_hash()` | 消除4处重复 |
| 3 | `shared/serialization.py` — 统一 `format_dt()` + `json_serializer()` | 消除6处重复 |
| 4 | `shared/pagination.py` — 统一分页 | 消除散落分页 |
| 5 | `shared/result.py` — 统一 `success()/fail()` | 响应格式基础 |
| 6 | **批量生成全部枚举**：TaskStatus/TaskType/PartyStatus/AnchorStatus、GroupStatus/GroupApprovalStatus/MemberRole/AuthStatus、NodeStatus/ActivationStatus/NodeLoadStatus/NodeType、UserRole/ScopeType/UserStatus、AgencyType/AgencyLevel/AgencyStatus、AnchorStatus/VerifyStatus/ChainType、TemplateStatus、DatasetStatus | 所有字符串魔法值类型化 |
| 7 | **批量生成全部Port接口**：TaskRepository/TaskResultRepository/TaskPartyRepository、GroupRepository/GroupMemberRepository/GroupResourceRepository、AgencyRepository/AgencyHierarchyPort、NodeRepository/NodeAgentPort、UserRepository/AccessControlPort/AuditLogPort、ChainAnchorPort/ChainRecordRepository、ComputationPort、DatasetRepository、TemplateRepository、AuditLogRepository | 全部抽象定义 |
| 8 | **批量生成全部领域异常**：InvalidStatusTransition/TaskExecutionDenied/CoreTemplateProtected/GroupNotActive/InvalidGroupOperation/ApprovalNotAllowed/NodeNotReady/InvalidNodeOperation | 异常体系 |
| 9 | **批量生成全部领域事件**：TaskRunning/TaskSucceeded/TaskFailed/ResultAnchored、GroupApproved/GroupRejected/MemberAdded/ResourceAuthorized | 事件定义 |
| 10 | 旧代码中4处重复哈希/序列化替换为shared引用 | 行为不变 |

### 关键策略
- 枚举/Port/异常/事件全部由AI根据旧代码中的字符串常量和Service方法签名**一次性批量生成**，无需逐个手写
- 此时不写任何实现代码，只定义接口

### 验收
- [ ] 新目录结构存在，旧代码不受影响
- [ ] 所有枚举/Port/异常/事件文件可import无报错

---

## Day1 下午（4h）：4个简单上下文批量迁移

> Template/Dataset/Chain/Computation 逻辑简单且互不依赖，AI并行生成

### P1 Template（~1h）

| # | 任务 |
|---|------|
| 1 | `domain/models.py` — StatTemplate富模型（`is_core_template()/enable()/disable()`） |
| 2 | `application/use_cases.py` — CreateTemplateUC, UpdateTemplateUC, DeleteTemplateUC |
| 3 | `application/dtos.py` — TemplateDTO |
| 4 | `adapters/persistence.py` — SQLAlchemyTemplateRepository |
| 5 | `adapters/schemas.py` + `adapters/api.py` — 从旧代码迁移，移除db.query() |
| 6 | main.py路由切换 + 删除旧代码 |

### P2 Dataset（~1h）

| # | 任务 |
|---|------|
| 1 | domain/ 富模型+UseCase+DTO+Repo+API，同P1模式 |
| 2 | main.py路由切换 + 删除旧代码 |

### P6 Chain（~1h）

| # | 任务 |
|---|------|
| 1 | `domain/models.py` — ChainRecord富模型（`mark_pending()/mark_success()/mark_failed()`）+ ContractInfo |
| 2 | `adapters/fisco_anchor.py` — FiscoAnchorAdapter |
| 3 | `adapters/mock_anchor.py` — MockAnchorAdapter（从ChainRecordService.mock_anchor_content()剥离） |
| 4 | `adapters/persistence.py` + `adapters/api.py` — 迁移 |
| 5 | 删除 chain_record_service.py + blockchain_anchor_service.py + resource_chain_service.py |

### P9 Computation（~1h）

| # | 任务 |
|---|------|
| 1 | `domain/services.py` — TaskExecutionService领域服务 |
| 2 | `adapters/secretflow_stat.py` — SecretFlowStatAdapter |
| 3 | `adapters/secretflow_fl.py` — SecretFlowFLAdapter |
| 4 | `adapters/mock_computation.py` — MockComputationAdapter（从task_service._mock_run_*剥离） |
| 5 | 删除 secretflow_stat_service.py + secretflow_fl_service.py |

### 关键策略
- 4个上下文**结构相同**，AI按P1模板批量生成P2/P6/P9
- Chain和Computation的核心收益是**Mock与生产分离**，这是最明显的架构改善

### 验收
- [ ] 模板CRUD / 数据集CRUD / 存证查询+锚定 / Mock计算 均回归通过
- [ ] 无Mock逻辑残留在生产代码中

---

## Day2 上午（4h）：Agency + Node

> 中等复杂度，需处理树形查询和外部调用

### P3 Agency（~2h）

| # | 任务 | 关键点 |
|---|------|--------|
| 1 | `domain/models.py` — Agency富模型（`disable()/enable()/is_ancestor_of()`） | 树形逻辑内聚 |
| 2 | `application/use_cases.py` — CreateAgencyUC, UpdateAgencyUC, DisableAgencyUC, DeleteAgencyUC | 拆分God方法 |
| 3 | `adapters/persistence.py` — SQLAlchemyAgencyRepository + SQLAlchemyAgencyHierarchyAdapter（BFS递归） | 隔离inspector反射 |
| 4 | `adapters/api.py` — 迁移 | 瘦API |
| 5 | 删除 agency_service.py(67KB) + agency_api.py | -67KB |

### P4 Node（~2h）

| # | 任务 | 关键点 |
|---|------|--------|
| 1 | `domain/models.py` — Node富模型（`start_activation()/complete_activation()/fail_activation()/deactivate()`） | 激活状态机 |
| 2 | `application/use_cases.py` — CheckNodeUC, ActivateNodeUC, DeactivateNodeUC | 用例拆分 |
| 3 | `adapters/agent_client.py` — HttpNodeAgentAdapter + MockNodeAgentAdapter | 隔离requests调用 |
| 4 | `adapters/persistence.py` + `adapters/api.py` — 迁移 | |
| 5 | 删除 node_service.py(27KB) + node_api.py | -27KB |

### 验收
- [ ] 机构CRUD+树形+级联删除 / 节点CRUD+Agent检测+激活/停用 回归通过
- [ ] `requests`库仅出现在 `adapters/agent_client.py`
- [ ] SQLAlchemy inspector 仅出现在 Adapter 层

---

## Day2 下午（4h）：Identity + Audit

> **关键里程碑**：AccessControlPort和AuditLogPort就绪，解锁Group和Task迁移

### P5 Identity（~3h）

| # | 任务 | 关键点 |
|---|------|--------|
| 1 | `domain/models.py` — User富模型（`lock()/unlock()/archive()/is_active()`）+ RoleBinding值对象 | 用户状态机 |
| 2 | `domain/services.py` — PermissionEvaluator（纯逻辑，不查DB） | 权限计算与查询分离 |
| 3 | `application/use_cases.py` — LoginUC, GetUserContextUC, BindRoleUC, ManageUserUC | |
| 4 | `adapters/persistence.py` — SQLAlchemyUserRepository + **SQLAlchemyAccessControlAdapter** + **SQLAlchemyAuditLogAdapter** | 核心Port实现 |
| 5 | `adapters/api.py` — 从 auth_api.py + user_api.py(27KB) 迁移 | |
| 6 | 删除 auth_service.py + access_control_service.py + user_api.py | |

### P7 Audit（~1h）

| # | 任务 |
|---|------|
| 1 | domain/ + application/ + adapters/ 完整迁移 |
| 2 | 删除 audit_log_service.py |

### 验收
- [ ] 登录/用户管理/角色绑定/菜单 全部回归通过
- [ ] AccessControlPort 可被 Group/Task/Node import
- [ ] AuditLogPort 可被 Group/Task/Agency import
- [ ] `is_ancestor_agency` 递归查询仅在 Adapter 层

---

## Day3 上午（4h）：Group + Task

> 最复杂的两个业务域，重构核心收益点

### P8 Group（~2h）

| # | 任务 | 关键点 |
|---|------|--------|
| 1 | `domain/models.py` — Group富模型（9个生命周期方法）+ GroupMember/Node/Dataset/Template子实体 | 状态机内聚 |
| 2 | `application/use_cases.py` — **拆分God方法**：CreateGroupUC, ApproveGroupUC, RejectGroupUC, DissolveGroupUC, AddMemberUC, AuthorizeNodeUC, AuthorizeDatasetUC, AuthorizeTemplateUC, ManageGroupUserUC, DeleteGroupUC | 9个UC替代1个67KB文件 |
| 3 | `adapters/persistence.py` — SQLAlchemyGroupRepository | |
| 4 | `adapters/api.py` — 从 group_api.py(37KB) 迁移 | |
| 5 | 删除 group_service.py(67KB) + group_api.py(37KB) + group_lifecycle_service.py | -105KB |

### P10 Task（~2h）

| # | 任务 | 关键点 |
|---|------|--------|
| 1 | `domain/models.py` — Task富模型（`start()/succeed()/fail()/cancel()/add_party()`）+ TaskResult富模型（`mark_anchoring()/mark_anchor_success()/mark_anchor_failed()`）+ TaskParty | 聚合根+状态守卫 |
| 2 | `application/use_cases.py` — CreateTaskUC, **RunTaskUC**, ManagePartyUC, QueryTaskResultUC | RunTaskUC编排5个Port |
| 3 | `adapters/persistence.py` — SQLAlchemyTaskRepository + SQLAlchemyTaskResultRepository | |
| 4 | `adapters/api.py` — 从 task_api.py(80KB) 迁移，移除所有编排逻辑 | 瘦API ~200行 |
| 5 | 删除 task_service.py + task_result_service.py + task_api.py(80KB) | -80KB |

### 验收
- [ ] 群组全生命周期 + 资源授权 + 用户管理 回归通过
- [ ] 任务CRUD + 参与方 + 执行(T1+T2) + 结果查询 回归通过
- [ ] Group/Task 富模型状态守卫有单元测试
- [ ] task_api.py < 300行, group_api.py < 400行

---

## Day3 下午（4h）：收尾

### P11 API瘦化+响应统一（~1h）

| # | 任务 |
|---|------|
| 1 | 所有端点统一 `success(data)/fail(msg)` 响应 |
| 2 | 所有列表端点统一 `PaginatedResponse` |
| 3 | 所有端点指定 `response_model` |

### P12 ORM映射隔离（~1.5h）

| # | 任务 |
|---|------|
| 1 | 各上下文 `adapters/orm_models.py` — SQLAlchemy ORM模型 |
| 2 | Repository 添加 `_to_domain()/_to_orm()` 双向转换 |
| 3 | `domain/models.py` 清理为纯Python（移除SQLAlchemy依赖） |
| 4 | Alembic env.py 适配 |

### P13 领域事件（~0.5h）

| # | 任务 |
|---|------|
| 1 | `shared/domain_events.py` — DomainEventBus |
| 2 | TaskSucceeded → 触发存证+审计 |
| 3 | main.py 事件订阅绑定 |

### P14 清理回归（~1h）

| # | 任务 |
|---|------|
| 1 | 删除旧 `app/models/` + `app/services/` + `app/schemas/` + `app/api/` |
| 2 | 旧 `core/` 移入 `infrastructure/` |
| 3 | 全量API回归测试 |
| 4 | 性能对比 |

### 验收
- [ ] `domain/` 目录下无 SQLAlchemy import
- [ ] 所有API响应结构一致
- [ ] 旧代码零残留
- [ ] 关键API性能不低于重构前

---

## 时间线

```
Day1
09:00 ──┬── P0 骨架+枚举+Port+异常+事件+shared（全部抽象定义）
13:00 ──┬── P1 Template ─┐
        ├── P2 Dataset  ─┤ 并行
        ├── P6 Chain    ─┤
        └── P9 Computation─┘

Day2
09:00 ──┬── P3 Agency（树形+级联）
11:00 ──┬── P4 Node（AgentPort+状态机）
13:00 ──┬── P5 Identity（AccessControlPort+AuditLogPort）  ← 关键里程碑
16:00 ──┬── P7 Audit

Day3
09:00 ──┬── P8 Group（拆分God方法+生命周期富模型）
11:00 ──┬── P10 Task（RunTaskUC+富模型+多Port编排）
13:00 ──┬── P11 响应统一
14:00 ──┬── P12 ORM隔离
15:30 ──┬── P13 事件驱动
16:00 ──┬── P14 清理回归
17:00 ─── 完成
```

---

## AI提效策略

| 策略 | 说明 | 节省时间 |
|------|------|----------|
| **批量生成枚举/Port/异常** | 旧代码中所有字符串状态→枚举，所有Service方法签名→Port接口，一次性生成 | 省2h逐个手写 |
| **模板化迁移** | P1走通后，P2/P6/P9按相同三层结构(domain/application/adapters)批量生成 | 省4h重复劳动 |
| **富模型方法提取** | 将Service中的if/else状态判断→领域模型方法，AI可从旧代码直接翻译 | 省3h手写状态机 |
| **ORM双向转换** | _to_domain()/_to_orm() 字段映射机械操作，AI按模型定义批量生成 | 省2h手写映射 |
| **旧代码对照而非重写** | 每个UseCase从旧Service方法搬逻辑，AI做"搬+改"而非"凭空写" | 降低遗漏风险 |

---

## 风险控制

| 风险 | 缓冲 |
|------|------|
| Identity权限回归不全 | Day2下午预留30min写权限矩阵测试 |
| Group/Task God方法拆分遗漏 | 每个UC完成后立即对照旧代码逻辑分支验证 |
| ORM隔离导致Alembic断裂 | Day3下午先在独立分支验证再合并 |
| 3天完不成 | P12(ORM隔离)和P13(事件)可延后，不影响核心架构；P11-P14约占3h，挪到第4天即可 |
