# 2026-05-15

## 第3阶段：群组基础管理与生命周期起步

### 新增文件
- backend/app/schemas/group_schema.py - 群组 Pydantic Schema (GroupCreate, GroupUpdate)
- backend/app/schemas/group_lifecycle_schema.py - 生命周期日志 Schema
- backend/app/services/group_service.py - 群组服务（list_groups, get_group_detail, create_group_with_creator_admin, update_group_basic_info, list_group_members, list_group_users, list_group_nodes）
- backend/app/services/group_lifecycle_service.py - 生命周期日志查询服务
- backend/app/api/group_api.py - 8个群组接口（列表/详情/创建/编辑/生命周期日志/成员/用户/节点）
- frontend/src/api/group.ts - 群组前端 API（含完整 TypeScript 类型定义）
- frontend/src/views/Group/GroupListView.vue - 群组列表页（搜索/筛选/分页/创建弹窗）
- frontend/src/views/Group/GroupDetailView.vue - 群组详情页（基础信息/统计卡片/5个Tab）
- backend/check_stage3.py - 自动化检测脚本（38项全部通过）

### 修改文件
- backend/app/main.py - 注册 group_api.router
- backend/app/services/access_control_service.py - 添加 group:read/create/update/manage 权限到 agency admin 和 group admin；修复 get_accessible_group_ids 不再过滤 active 状态
- backend/app/services/menu_service.py - 群组管理菜单路径从 /group-manage 改为 /groups
- frontend/src/router/index.ts - 添加 /groups 和 /groups/:id 路由，/group-manage 重定向到 /groups

### 关键修复
- get_accessible_group_ids 只返回 active 群组导致创建人看不到自己创建的 draft 群组 → 改为返回所有加入群组（不限状态）

### 检测结果（Stage 3）
- 38 PASS, 0 FAIL

---

## 第4阶段：群组创建权限、审批起步与成员/用户/节点授权配置

### 新增文件
- backend/app/services/access_control_service.py（第4阶段新增函数） - is_platform_admin, is_agency_admin, is_group_admin, is_ancestor_agency, is_same_level_agency, find_common_parent_agency, can_approve_group
- backend/app/services/group_lifecycle_service.py（重写） - log_lifecycle_event, 完整生命周期日志记录

### 修改文件
- backend/app/services/group_service.py（重大更新） - 新增 create_group_with_creator_admin（权限判断+审批逻辑+自动绑定创建人）, approve_group, reject_group, add_group_member, remove_group_member, add_group_user, update_group_user_role, remove_group_user, add_group_node, remove_group_node, list_available_group_nodes
- backend/app/schemas/group_schema.py（重写） - GroupCreate, GroupUpdate, GroupApprove, GroupReject, AddGroupMember, AddGroupUser, UpdateGroupUserRole, AddGroupNode
- backend/app/api/group_api.py（重写） - 18个 REST 接口（列表/创建/详情/编辑/审批/驳回/生命周期日志/成员CRUD/用户CRUD+角色修改/节点CRUD+可授权列表）
- frontend/src/api/group.ts（重写） - 完整 TypeScript 类型定义 + 18个 API 函数
- frontend/src/views/Group/GroupListView.vue（重写） - 搜索/筛选/分页/创建弹窗/审批按钮/状态标签
- frontend/src/views/Group/GroupDetailView.vue（重写） - 4个Tab（成员机构/用户授权/节点授权/生命周期日志）+ CRUD弹窗 + 懒加载

### 关键设计决策
- 权限模型：platform_admin 直接创建(draft) / agency_admin 上级直接创建(draft) / agency_admin 同级协作(pending_approval) / group_admin 禁止(403) / 普通用户禁止(403)
- 审批通过后状态从 pending_approval → draft
- 创建群组时自动绑定：牵头机构到 group_member + 创建人到 sys_user_group + 创建人 admin 角色到 sys_user_role_binding
- 机构层级：市(city,parent=None) → 区县(county,parent=city_id)，is_ancestor_agency 递归判断

### 数据修复
- agency_admin(id=13): agency_id=8→10, status=disabled→active, password 重新哈希
- agency(id=11,12): parent_agency_id 设置为 10, agency_level 设为 county
- 添加 platform_admin:admin:platform 和 agency_admin:admin:agency:10 的 role_binding

### 检测结果（Stage 4）
- 38 PASS, 0 FAIL（check_stage4.py）
